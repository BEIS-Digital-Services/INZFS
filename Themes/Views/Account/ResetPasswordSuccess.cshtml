
@{
    ViewLayout = "LayoutRegistration";
    ViewData["Title"] = "Password updated";
}

<main class="govuk-main-wrapper " id="main-content" role="main">
    <div class="govuk-grid-row">
        <div class="govuk-grid-column-two-thirds">
            <h1 class="govuk-heading-xl">@ViewData["Title"]</h1>
            <p class="govuk-body">
                Your password has been updated and you can sign in
            </p>
            <a href="@Url.Action("Login", "Account", new {area = "INZFS.Theme", returnUrl=Url.GetQuery("returnUrl")})">
                <button class="govuk-button govuk-button govuk-!-margin-top-6" data-module="govuk-button">
                    Sign in to your account
                </button>
            </a>
        </div>
    </div>
</main>