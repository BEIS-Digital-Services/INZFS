version: '3.7'

# volumes:
#     prometheus_data: {}

services:
  prometheus:
    image: ${DOCKER_REGISTRY-}prom-blackbox
    build:
      context: ./
      dockerfile: ./prometheus/Dockerfile1
    ports:
      - 9115:9115
    command:
      - '--config.file=/etc/blackboxexporter/config.yml'
    restart: always
    volumes:
      - ./config.yml:/etc/blackboxexporter/config.yml:ro
      # - prometheus_data:/prometheus