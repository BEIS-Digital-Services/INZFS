global:
  resolve_timeout: 1m

route:
  # group_by: ['app']
  receiver: 'zulip-notifications'

receivers:
- name: 'zulip-notifications'
  webhook_configs:
  - url: 'https://eef.zulipchat.com/api/v1/external/alertmanager?api_key=jrE5IrlmicCWIsV8cmY7kXjsmGqAyVoq=stream%20name&name=host&desc=alertmanager'
    send_resolved: true