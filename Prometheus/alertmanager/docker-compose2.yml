version: '3.4'

volumes:
  alertmanager_data: {}

services:
  alertmanager:
    image: ${DOCKER_REGISTRY-}prom-alertmanager2
    build:
      context: ./
      dockerfile: ./Dockerfile
    container_name: prom-alertmanager2
    ports:
      - 9093:9093
      - 9094:9094
    restart: always
    volumes:
    - ./alertmanager.yml:/etc/alertmanager/alertmanager.yml:ro
    - ./alertmanager_data:/alertmanager
    - ./templates:/etc/alertmanager/templates
    command:
    - '--config.file=/etc/alertmanager/alertmanager.yml'
    - '--storage.tsdb.path=/alertmanager'
    - '--web.external-url=http://inzfs-prom-alertmanager2.london.cloudapps.digital/api/v2/alerts'
    - '--web.listen-address=inzfs-prom-alertmanager2.london.cloudapps.digital:9093'
    - '--cluster.listen-address=inzfs-prom-alertmanager2.london.cloudapps.digital:9094'
    - '--cluster.settle-timeout=2m'
    - '--cluster.peer=inzfs-prom-alertmanager1.london.cloudapps.digital:9094'
    - '--cluster.advertise-address=inzfs-prom-alertmanager2.london.cloudapps.digital:9094'